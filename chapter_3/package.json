{
  "name": "chapter_3",
  "version": "1.0.0",
  "description": "A full stack todo application that uses NodeJS backend, a SQLite db and JWT auth.",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "dev": "node --watch --env-file=.env ./src/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "better-sqlite3": "^12.4.1",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2"
  }
}
